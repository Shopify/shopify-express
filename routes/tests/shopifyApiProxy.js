const shopifyApiProxy = require('../shopifyApiProxy');

describe('shopifyApiProxy', async () => {
  it.skip('proxies requests with credentials from session');
  it.skip('does not proxy requests to dissallowed urls');
  it.skip('does not proxy requests urls which contain dissallowed urls');
});
